name: danef_dictionary_admin_panel
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  form_field_validator: ^1.0.1
  http: ^0.12.2
  get_it: ^4.0.4
  google_fonts: ^1.1.0
  shared_preferences: ^0.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
